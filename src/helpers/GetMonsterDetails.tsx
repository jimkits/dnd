import MonsterNotFound from "../data/MonsterNotFound";

function GetHeroDetails(hero: string) {
    if (hero.toLowerCase() === "fighter") {
        return MonsterNotFound;
    }
    else {
        return MonsterNotFound;
    }
}

export default GetHeroDetails;